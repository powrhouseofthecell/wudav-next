import Image from 'next/image';

const Client = ({ img1, img2, img3 }) => {
   return (
      <>
         <h1 className="my-14 text-center text-3xl font-extrabold leading-9 tracking-tight text-gray-900 dark:text-gray-100 sm:text-4xl sm:leading-10 md:text-6xl md:leading-14"></h1>
         {/* <Image alt="Client Image" className="" width={1300} height={500} src={img1} />
         <Image alt="Client Image" className="" width={1300} height={500} src={img2} />
         <Image alt="Client Image" className="" width={1300} height={500} src={img3} /> */}
      </>
   );
};

export default Client;
