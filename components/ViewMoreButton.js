import styles from '../css/viewmorebutton.module.css';

const ViewMoreButton = () => {
   return <button className={`${styles.btn} ${styles.draw_border}`}>Draw Border</button>;
};

export default ViewMoreButton;
